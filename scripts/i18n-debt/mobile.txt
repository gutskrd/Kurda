# English the phone still renders. Currently: none.
#
# This file is empty and stays. It was deleted once before, in #721, on the
# strength of a gate that could not see a `label` prop, a string inside a JSX
# expression, or anything at all in a plain .ts module — 83 pieces of English
# were on screen at the time, and the empty file said otherwise.
#
# So it is kept, empty, as the place the number lives. Anything added here has
# to be paid off; an entry that stops matching is itself an error, so the list
# cannot quietly become a list of things that used to be true.
#
# How it was paid down, for whoever needs the same again:
#   83 → 67 → 48 → 98 (when .ts opened) → 91 → 70 → 49 → 28 → 0
# The jump to 98 is the honest part — none of those fifty were new.
