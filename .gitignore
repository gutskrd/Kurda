node_modules/
dist/
coverage/
*.log
.env
.env.*
!.env.example
.idea/
.vscode/
.DS_Store
.expo/

api/.pgdata/

# The demo site is its own standalone repo (preserved separately) — never
# absorb it into this monorepo. See Demo-MyKurda/README.md.
/Demo-MyKurda/

# Cosmetic source assets — canonical copies live in web/public/cosmetics (avatars/icons)
# and Cloudflare R2 (backgrounds, via api/scripts/upload-cosmetics). Keys/manifest are
# committed (api/content/cosmetics.json); the large/binary sources are not.
/default-avatars/
/premium-icons/
/mykurda-background/

# Where source artwork is dropped for conversion (e.g. sticker PNGs). The files
# that ship are the sized WebPs under web/public/stickers, generated from these.
# Ignored so a `git add -A` cannot sweep multi-megabyte originals into a commit.
/stickers/
