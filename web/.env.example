# Base URL of the MyKurda API (Render). No trailing slash.
# Production default (baked in src/lib/config.ts) is https://kurda-api.onrender.com
# For local dev against a locally-running API, use http://localhost:3000
VITE_API_URL=https://kurda-api.onrender.com
